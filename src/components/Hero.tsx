import React from 'react';
import './Hero.css'; // Assuming we will create a CSS file for styling

const Hero: React.FC = () => {
    return (
        <div className="hero">
            <h1>Welcome to Nuestro Pulso</h1>
            <p>Your gateway to Colombian culture and heritage.</p>
        </div>
    );
};

export default Hero;